name: jezail_ui
description: "Jezail - Android penetration testing toolkit UI"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  http: ^1.5.0
  web: ^1.1.1
  mime: ^2.0.0
  file_picker: ^10.3.1
  url_launcher: ^6.3.2
  logger: ^2.6.1
  go_router: ^14.2.1
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  dead_code_analyzer: ^1.1.0

flutter:
  uses-material-design: true
